<%= simple_form_for @contact_message, url: contact_path, html: { class: 'form' } do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :message, as: :text, input_html: { rows: 10} %>
  <%= f.button :submit, value: 'Send' %>
<% end %>
